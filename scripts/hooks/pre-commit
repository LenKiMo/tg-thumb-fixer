#!/bin/sh
# 本地隐私门禁：提交前扫描跟踪文件里的本机路径痕迹，命中即中止本次提交。
# 启用（仓库根目录执行一次）：git config core.hooksPath scripts/hooks
python scripts/check_privacy.py || {
  echo "→ 隐私门禁拦截：请把本机绝对路径改成无盘符占位（如 目录A/子目录）后重新提交。" >&2
  exit 1
}
